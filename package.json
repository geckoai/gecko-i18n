{
  "name": "@geckoai/gecko-i18n",
  "version": "1.0.0",
  "description": "",
  "main": "dist/cjs",
  "types": "dist/types",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:clean": "rimraf dist && rimraf docs",
    "build:cjs": "tsc --project tsconfig.prod.cjs.json",
    "build:amd": "tsc --project tsconfig.prod.amd.json",
    "build:esm": "tsc --project tsconfig.prod.esm.json",
    "build:types": "tsc --project tsconfig.prod.types.json",
    "build:docs": "typedoc --options ./typedoc.json",
    "release": "pnpm run build:cjs && pnpm run build:amd && pnpm run build:esm && pnpm run build:types "
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/geckoai/gecko-i18n.git"
  },
  "devDependencies": {
    "@types/react": "^19.1.6",
    "typescript": "4.9.5",
    "react": "^19.1.0"
  },
  "peerDependencies": {
    "react": ">= 16",
    "typescript": "^3.2.1 || ^4"
  },
  "dependencies": {
    "@geckoai/class-mirror": "^1.0.2",
    "@geckoai/gecko-core": "^1.0.0",
    "@geckoai/gecko-router": "^1.0.0"
  },
  "license": "MIT"
}
